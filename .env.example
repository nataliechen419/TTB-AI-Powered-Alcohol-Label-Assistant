# Copy this file to .env and fill in a real key. .env is gitignored — it
# will never be committed.
#
# Required — the app has no fallback mode and every extraction call fails
# without a valid key.
ANTHROPIC_API_KEY=
