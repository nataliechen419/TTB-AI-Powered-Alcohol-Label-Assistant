node_modules/
dist/
build/
.cache/

# Ignore all .env variants except the checked-in template that documents
# which vars are needed (see .env.example).
.env
.env.*
!.env.example

.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Vercel CLI's local project link (org/project IDs) — regenerated per machine.
.vercel

# Editor/OS clutter.
.DS_Store
.claude/
